/// Returns a human-readable jurisdiction name for the given administrative code.
/// TODO: populate the mapping once the ordinance schema is finalised (Phase 4).
pub fn name_for(_code: &str) -> Option<&'static str> {
    None
}
