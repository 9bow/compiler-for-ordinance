/// Converts ordinance XML bytes to a Markdown string.
/// Placeholder: always returns an empty string until the ordinance pipeline is implemented.
pub fn xml_to_markdown(_xml: &[u8]) -> anyhow::Result<String> {
    Ok(String::new())
}
