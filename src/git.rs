/// Initialises a bare Git repository at the given path.
/// Placeholder: no-op until the ordinance pipeline is implemented.
pub fn init_bare(_path: &std::path::Path) -> anyhow::Result<()> {
    Ok(())
}
