/// Returns a list of cached ordinance XML filenames under the given directory.
/// Placeholder: always returns an empty list until the ordinance pipeline is implemented.
pub fn list_cached(_dir: &std::path::Path) -> anyhow::Result<Vec<String>> {
    Ok(vec![])
}
